The DNDD marketing site — a single static page, deployed to GitHub Pages.

Public because Pages is free on public repositories and the source is marketing
HTML. **The product repository is private and separate.**
